知识点：
1、Widget blueprints是从基础c++类UUserWidget派生出来的。

2、UI包括两个部分：
(1)、View(视图)：展示数据的视觉效果：健康条，能力图标......等控件。

(2)、Model(模型)：数据本身

要连接这两个部分，需要WidgetController(属于控制器类):从模型中获取数据，并将其广播到视图。

它的任务其一是：
通过玩家组件互动促使模型发生变化。

其二是：
从模型中获取数据，并将其广播到视图。

